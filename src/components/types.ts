export enum ESortBy {
  NONE = 'none',
  FIRST_NAME = 'firstName',
  LAST_NAME = 'lastName',
  COUNTRY = 'country',
}

export enum ESortOrder {
  DESC = 'desc',
  ASC = 'asc',
}

export enum EPageDirection {
  NEXT = 'next',
  PREV = 'prev',
}
